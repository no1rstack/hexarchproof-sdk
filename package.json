{
  "name": "hexarchproof-sdk-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run compile -w packages/sdk && npm run compile -w packages/vscode-extension",
    "build:sdk": "npm run compile -w packages/sdk",
    "build:extension": "npm run compile -w packages/vscode-extension",
    "test": "npm test -w packages/sdk",
    "lint": "npm run lint -w packages/sdk && npm run lint -w packages/vscode-extension"
  }
}
